<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@android:color/holo_blue_light</item>
        <item name="android:textColor">#FFF</item>
    </style>

    <!-- Custom theme to set color on actionBar-->
    <style name="customActionBar" parent="Theme.AppCompat.Light">
        <item name="android:background">@android:color/holo_blue_light</item>
    </style>

    <!-- Custom theme to removing mainActivity's actionBar-->
    <style name="customFullscreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@android:color/holo_blue_light</item>
        <item name="android:textColor">#FFF</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Custom Alert Dialog -->
    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">#5fa3d0</item>
        <item name="android:textColor">#33b5e5</item>
        <item name="android:background">#000000</item>
        <item name="android:windowTitleStyle">@style/DialogTitleCustom</item>
    </style>

    <!-- Custom Dialog Title used by AppCompatAlertDialogStyle -->
    <style name="DialogTitleCustom">
        <item name="android:textColor">@android:color/holo_blue_light</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
